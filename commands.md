--------------------------------------------Docker-commands--------------------------------------------------------------------
# to know the ip addressesthat the containers run  inside the vm.

docker inspect bridge
